import { Box } from "native-base"

interface ForgotPasswordProps { }
const ForgotPassword = ({ }: ForgotPasswordProps) => {
  return (
    <Box>ForgotPassword Component</Box>
  )
}

export default ForgotPassword

